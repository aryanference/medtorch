[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "torchio"

[project]
name = "medtorch"
version = "1.2.0"
description = "Advanced 3D medical imaging toolkit with CT and temporal augmentations"
readme = "README.md"
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
    "Typing :: Typed",
]
keywords = ["medical", "image processing", "pytorch", "augmentation", "ct", "mri"]
requires-python = ">=3.10"
dependencies = [
    "deprecated>=1.2",
    "einops>=0.3",
    "humanize>=0.1",
    "jaxtyping>=0.2",
    "nibabel>=3",
    "numpy>=1.20",
    "packaging>=20",
    "rich>=10",
    "scipy>=1.7",
    "simpleitk >=1.3, !=2.0.*, !=2.1.1.1",
    "torch>=1.9",
    "tqdm>=4.40",
    "typer>=0.1",
]

[project.optional-dependencies]
csv = ["pandas>=1"]
plot = ["colorcet", "matplotlib>=3.4"]
video = ["ffmpeg-python>=0.2.0"]
sklearn = ["scikit-learn>=1.6.1"]
monai = ["monai>=1.4"]
all = [
    "medtorch[csv]",
    "medtorch[monai]",
    "medtorch[plot]",
    "medtorch[sklearn]",
    "medtorch[video]",
]

[project.scripts]
# MedTorch-native CLI names
medtorch-info = "torchio.cli.print_info:app"
medtorch-transform = "torchio.cli.apply_transform:app"
# Backward-compatible aliases
tiohd = "torchio.cli.print_info:app"
tiotr = "torchio.cli.apply_transform:app"
torchio-transform = "torchio.cli.apply_transform:app"

[project.urls]
Homepage = "https://github.com/medtorch/medtorch"
Source = "https://github.com/medtorch/medtorch"
"Issue tracker" = "https://github.com/medtorch/medtorch/issues"
Documentation = "https://github.com/medtorch/medtorch#readme"
"Release notes" = "https://github.com/medtorch/medtorch/releases"

[dependency-groups]
dev = [
    { include-group = "doc" },
    { include-group = "maintain" },
    { include-group = "quality" },
    { include-group = "test" },
    "ipykernel",
    "ipywidgets",
    "prek",
]
doc = [
    "colorcet>=3.1.0",
    "distinctipy",
    "einops",
    "matplotlib",
    "mkdocstrings-python>=2.0.2",
    "zensical>=0.0.23",
]
maintain = ["bump-my-version"]
quality = ["ruff"]
types = [
    "matplotlib",
    "ty",
    "pandas-stubs",
    "pillow",
    "pip",
    "types-deprecated",
    "types-tqdm",
]
test = [
    "coverage",
    "einops",
    "matplotlib",
    "monai>=1.4",
    "parameterized",
    "pillow",
    "pytest>=9",
    "pytest-sugar",
    "scikit-learn",
    "tox-uv",
]

[tool.bumpversion]
current_version = "1.2.0"
commit = true
tag = true

[[tool.bumpversion.files]]
filename = "src/torchio/__init__.py"
search = "__version__ = '{current_version}'"
replace = "__version__ = '{new_version}'"

[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

[tool.pytest.ini_options]
addopts = ["-ra", "--strict-config", "--strict-markers"]
filterwarnings = [
    # Ignore SimpleITK Swig warnings
    "ignore:builtin type .* has no __module__ attribute",
    "ignore:Casting complex values to real discards the imaginary part",
    # Raised by SimpleITK on CI
    "ignore:invalid escape sequence",
]
log_level = "INFO"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "serial",
]
minversion = "9"
testpaths = ["tests"]
xfail_strict = true

[tool.ruff.format]
quote-style = 'single'

[tool.ruff.lint]
preview = true
select = ["B", "E", "F", "I", "W"]
ignore = ["E203", "E501", "N813"]

[tool.ruff.lint.isort]
force-single-line = true

[tool.coverage.run]
omit = ["**/_remote_module_non_scriptable.py"]
